DATABASE_URL=
ANTHROPIC_API_KEY=

# Optional — IP-based rate limiting via Upstash Redis.
# If either value is absent the rate limiter is disabled and the app runs normally.
# Create a free Redis database at https://console.upstash.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
